interface Body {
    query: string;
}

export const serverGraphQL = {
    fetch: async () => {
        const res = await fetch('');
    }
};
/* export const server = {
    fetch: async () => {
        const res = await fetch();
    }
}; */